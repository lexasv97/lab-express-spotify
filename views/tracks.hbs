{{#each tracks}}

    <h3>{{name}}</h3>

    {{#if preview_url}}

        <audio src={{preview_url}} controls></audio>


        {{else}}

            <p>No track preview found</p>

    {{/if}}

{{/each}}